{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "api/Dockerfile",
        "context": "api"
    },
    "deploy": {
        "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    }
}