{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "webapp/api/Dockerfile",
        "context": "webapp/api"
    },
    "deploy": {
        "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
    }
}