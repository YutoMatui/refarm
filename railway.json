{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "api/Dockerfile"
    },
    "deploy": {
        "startCommand": "/bin/sh -c \"uvicorn app.main:app --host 0.0.0.0 --port $PORT\""
    }
}